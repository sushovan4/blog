$('document').ready(function( ){
    $('.ui.dropdown').dropdown( );

    $('.slick').slick({
	dots: true,
	arrows: true
    });
    
    // $(".menu .item")
    // 	.click(function() {
    // 	    $('html, body').animate({
    // 		scrollTop: $($(this).attr("href")).offset().top
    // 	    }, 1000);
    // 	});
});
